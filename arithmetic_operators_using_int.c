#include<stdio.h>
int main(){
    int x , y ;
    x = 5;
    y = 2;
    printf(" Additn of the given numbers is : %d \n", x + y);
    printf(" Subtractn of the given numbers is : %d \n", x - y);
    printf(" Multiple of the given numbers is : %d \n", x * y);
    printf(" Division of the given numbers is : %d \n", x / y);// IT WILL ONLY GIVE QOUTIENT
    printf(" Remainder of the given numbers is : %d ", x % y);
    return 0;
}    

