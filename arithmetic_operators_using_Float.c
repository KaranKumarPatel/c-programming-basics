#include<stdio.h>
int main(){
    float x , y ;
    x = 5;
    y = 2;
    printf(" Additn of the given numbers is : %f \n", x + y);
    printf(" Subtractn of the given numbers is : %f \n", x - y);
    printf(" Multiple of the given numbers is : %f \n", x * y);
    printf(" Division of the given numbers is : %f \n", x / y);
    return 0;
}    

